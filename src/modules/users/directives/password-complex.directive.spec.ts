import { PasswordComplexDirective } from './password-complex.directive';

describe('PasswordComplexDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordComplexDirective();
    expect(directive).toBeTruthy();
  });
});
